import * as React from 'react';

export const Home: React.VFC = () => (
  <div>
    <p>
      This is an example single page app built with React and React&nbsp;Router
      using BrowserRouter. Navigate with the links below and refresh the page or
      copy/paste the url to test out the redirect functionality deployed to
      overcome GitHub&nbsp;Pages incompatibility with single page apps (like
      this one).
    </p>
  </div>
);
